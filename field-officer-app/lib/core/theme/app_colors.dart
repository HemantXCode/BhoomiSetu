import 'package:flutter/material.dart';

class AppColors {
  // Primary Government of India / BhoomiSetu Saffron Palette
  static const Color primary = Color(0xFFD9531E); // Deep National Saffron
  static const Color primaryLight = Color(0xFFFF7A45);
  static const Color primaryDark = Color(0xFFA6360A);
  static const Color primaryContainer = Color(0xFFFFE8DC);

  // Secondary Navy Palette
  static const Color secondary = Color(0xFF0D1B2A); // Deep Navy
  static const Color secondaryLight = Color(0xFF1E3A8A);
  static const Color secondaryDark = Color(0xFF070E17);
  static const Color secondaryContainer = Color(0xFFE2E8F0);

  // Background & Surfaces (High contrast, outdoor friendly)
  static const Color background = Color(0xFFF4F6F8);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceElevated = Color(0xFFFFFFFF);
  static const Color cardBorder = Color(0xFFE2E8F0);

  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF64748B);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status & Verification Indicators
  static const Color success = Color(0xFF15803D); // Forest Green
  static const Color successBg = Color(0xFFDCFCE7);
  static const Color warning = Color(0xFFD97706); // Amber
  static const Color warningBg = Color(0xFFFEF3C7);
  static const Color danger = Color(0xFFDC2626); // Ruby Red
  static const Color dangerBg = Color(0xFFFEE2E2);
  static const Color info = Color(0xFF2563EB); // Royal Blue
  static const Color infoBg = Color(0xFFDBEAFE);

  // Status Badge Colors
  static const Color pending = Color(0xFFF59E0B);
  static const Color inProgress = Color(0xFF3B82F6);
  static const Color verified = Color(0xFF10B981);
  static const Color rejected = Color(0xFFEF4444);
  static const Color syncPending = Color(0xFF8B5CF6);
  static const Color synced = Color(0xFF059669);

  // Offline / Connectivity
  static const Color online = Color(0xFF10B981);
  static const Color offline = Color(0xFF6B7280);
  static const Color offlineBanner = Color(0xFF374151);

  // Shadows
  static const Color shadow = Color(0x0F000000);
}
